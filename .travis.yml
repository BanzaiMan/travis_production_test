language: cpp

compiler:
  - clang

install:
  - true

script:
  - echo $PATH
  - which clang
  - sudo which clang
  - sudo env | grep PATH
  - sudo -E env
  - sudo -E which clang
  - sudo -i which clang
  - sudo -i env | grep PATH
