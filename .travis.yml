language: ruby
sudo: false

rvm:
  - '2.1.6'

addons:
  apt:
    sources:
      - git-core
    packages:
      - git

notifications:
  email: false

install:
  - true
script:
  - cat /home/travis/.rvm/gemsets/global.gems
  - git --version
