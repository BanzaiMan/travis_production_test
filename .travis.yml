language: c
sudo: required
services:
  - docker

before_install:
  - sudo -E apt-add-repository -y ppa:chris-lea/zeromq
  - sudo apt-get update -qq

install:
  - sudo apt-get install pyzmq

script:
  - true

