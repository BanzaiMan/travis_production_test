compiler:
  - gcc
  - clang
env:
  - 
  - SANITIZE=address
  - SANITIZE=undefined
matrix:
  exclude:
    - compiler: gcc
      env: SANITIZE=address
    - compiler: gcc
      env: SANITIZE=undefined

script: true
