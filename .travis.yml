os: windows

language: node_js
node_js: 12

install: skip
script:
  - gem install travis
  - travis --version
  
notifications:
  email: false

# deploy:
#   provider: s3
#   edge: true
#   access_key_id: "YOUR AWS ACCESS KEY"
#   secret_access_key: "YOUR AWS SECRET KEY"
#   bucket: "S3 Bucket"
#   on:
#     all_branches: true
