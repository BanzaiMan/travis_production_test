language: php

php:
  - 5.6

notifications:
  email: false

sudo: false

before_install:
  - echo "extension=memcache.so" >> `php --ini | grep "Loaded Configuration" | sed -e "s|.*:\s*||"`

install:
  - true

script:
  - php --ini
