language: ruby

rvm:
  - default

sudo: required
dist: trusty

install:
  - true

script:
  - docker pull ubuntu
  - docker run -d --name ubuntu ubuntu
  - docker exec ubuntu apt-get install curl
  - docker exec ubuntu curl -I yum.puppetlabs.com
  - docker stop ubuntu

notifications:
  email: false

