language: ruby

matrix:
  include:
  - os: linux
    sudo: required
    dist: precise
    group: edge
  - os: linux
    services: docker
    group: edge
  - os: linux
    sudo: false

install: true

script:
  - dpkg -l | egrep '(tcl|tk)'