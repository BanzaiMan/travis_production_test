language: ruby

rvm:
  - '2.1.8'

matrix:
  include:
    - sudo: required
      dist: precise
    - sudo: required
      dist: trusty
    - sudo: false

notifications:
  email: false

script:
  - cat /home/travis/.rvm/gemsets/*.gems
  - git --version
  - ldd `which ruby`
