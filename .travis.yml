language: ruby

addons:
  postgresql: "9.3"

before_install:
  - ls -l /var/lib/postgresql/*
  - sudo cat /usr/share/postgresql-common/init.d-functions
  - sudo cat /etc/init.d/postgresql

script:
  - ps aux | grep -v grep | grep postgres
