language: go

addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

sudo: false


install:
  - true

script:
  - ls /etc/apt/sources.list.d/google*.list
  - cat /etc/apt/sources.list.d/google*.list
  - cat ~/apt-get-update.log
  - google-chrome --version
