language: objective-c

sudo: false

rvm:
  - 1.9.3
  
install: true

script: true

deploy:
  - provider: opsworks
    access_key_id: key
    secret_access_key: secret
    app-id: id
    on:
       branch: production
