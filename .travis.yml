language: c

sudo: required
dist: trusty

install:
  - true
script:
  - PATH=$(echo $PATH | tr ':' "\n" | sed ':/opt/python:d' | tr "\n" ":" | sed "s|::|:|g")
  - echo $PATH
  - which python
  - which python3
