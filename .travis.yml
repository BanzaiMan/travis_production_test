language: ruby

rvm:
  - 2.1.5
  - 2.2.0

install:
  - rm -rf foo* FOO*
  - touch foo FOO foo.txt foo.TXT FOO.txt FOO.TXT
  - ls -l foo* FOO* || true
  - df -hT

script:
  - ruby -ve 'p Dir.glob("*.txt")'
  - ruby -ve 'p Dir.glob("foo.txt")'
  - ruby -ve 'p Dir.glob("FOO.txt")'
