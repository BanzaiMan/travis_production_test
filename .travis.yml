language: php

before_install:
  - echo $PATH
  - which php
  - curl https://gist.githubusercontent.com/createproblem/eaea35a673e0c8696f2e/raw/php_install.sh | bash -x -s -- 5.5.9

install:
  - true

script:
  - echo $PATH
  - which php
