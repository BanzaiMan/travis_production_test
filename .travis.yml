language: node_js
node_js:
  - '4'
sudo: false

env:
  - NPM_VERSION=3.5.2
  - NPM_VERSION=3.5
  - NPM_VERSION=3

before_install:
  - npm install -g npm@$NPM_VERSION
  - npm --version

script:
  - true

