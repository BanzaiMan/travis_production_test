language: python

matrix:
  include:
    - sudo: required
      services:
        - docker
    - sudo: false
      addons:
        apt:
          packages:
            - alex

install:
  - true

script:
  - true
