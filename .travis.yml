language: java

sudo: false

rvm: default

env:
  global:
    - PATH=$HOME/.local/bin:$PATH

install:
  - which pip
  - which python
  - python --version
  - pip --version
  - pip install --ignore-installed --user pip
  - pip --version

script:
  - true


