language: ruby

sudo: false

install: true

script: true

deploy:
  - provider: script
    edge:
      source: travis-ci/dpl
    script: echo "foo"
  - provider: script
    edge:
      source: travis-ci/dpl
    script: echo "bar"

notifications:
  email: false
