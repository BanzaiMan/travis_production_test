language: ruby

rvm:
  - '2.1'

os:
  - linux
  - osx

install:
  - true

script:
  - if [ $TRAVIS_OS_NAME = 'linux' ]; then ls -lU; else ls -lf; fi
  - if [ $TRAVIS_OS_NAME = 'linux' ]; then df -aT; else df -a; fi

