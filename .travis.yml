language: php

sudo: false

php:
  - 5.5
  - hhvm
  - hhvm-nightly

install:
  - true
  
script:
  - phpenv version-name
  - ls ~/.phpenv/versions/$(phpenv version-name)/
  - ls ~/.phpenv/versions/$(phpenv version-name)/etc
