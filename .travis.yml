language: c

sudo: false

addons:
  apt:
    packages:
      - cmake
      - libsdl1.2-dev
      - libsdl-mixer1.2-dev
      - libsdl-image1.2-dev
      - libsdl-gfx1.2-dev 
      - libboost-dev

install:
  - true

script:
  - true
