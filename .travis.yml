language: ruby

sudo: false

services:
  - mysql
  - elasticsearch

addons:
  apt:
    sources:
      - elasticsearch-1.7
    packages:
      - elasticsearch

install:
  - true
script:
  - cat ~/apt-get-update.log

