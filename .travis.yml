language: ruby

rvm:
  - 2.1

sudo: false

addons:
  postgresql: '9.4'

services:
  - postgresql

notifications:
  email: false

before_install:
  - psql -U postgres -c 'create extension hstore;'

install:
  - true

script:
  - "psql -c \"select hstore_to_json('a=>1,a=>2'::hstore);\""
