language: ruby

sudo: required

services:
  - docker

before_install:
  - docker pull mysql:5.6
  - docker run --name mysql -e MYSQL_ROOT_PASSWORD=my-secret-pw -d mysql:5.6
install: true

script:
  - docker exec mysql mysql -u root -p my-secret-pw 'SHOW VARIABLES LIKE "%version%";'
