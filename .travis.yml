language: ruby

sudo: false

before_install:
  - ruby -rresolv -e 'resolver=Resolv::DNS.new(:nameserver => ["8.8.8.8"]); puts resolver.getaddress("www.cs.utah.edu")'
  - curl -L -o installer.sh http://www.cs.utah.edu/plt/snapshots/current/installers/min-racket-current-x86_64-linux-precise.sh

install:
  - true

script:
  - true
