language: php

sudo: required
dist: trusty

env:
  - FOO=bar
  - FOO=baz

install:
  - true

script:
  - touch file-$FOO

before_deploy:
  - ls file-$FOO

deploy:
  provider: releases
  api_key: "GITHUB OAUTH TOKEN"
  file: file-$FOO
  skip_cleanup: true
  on:
    tags: true
