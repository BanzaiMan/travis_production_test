language: php

sudo: false

install:
  - true

script:
  - php -r 'phpinfo();'
  
deploy:
  provider: gae
  edge:
    source: travis-ci/dpl
    branch: ha-test-pr-316
  project: foo
  default: false
  on:
    all_branches: true
