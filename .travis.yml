language: phyton
sudo: false

notifications:
  email: false

script:
  - true

matrix:
  include:
    - env: ENV=docs
      python: 2.7
      # Note: graphviz is needed for grapgs embedded in the docs
      addons:
        apt:
          packages:
            - graphviz