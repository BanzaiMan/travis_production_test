language: erlang

otp_release:
  - 17.0

before_install:
  - sudo apt-get install unixodbc-dev
  - kerl build $TRAVIS_OTP_RELEASE ${TRAVIS_OTP_RELEASE}_odbc
  - source $HOME/otp/${TRAVIS_OTP_RELEASE}_odbc/activate
  
script:
  - true
