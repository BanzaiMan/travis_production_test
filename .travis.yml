language: python

before_install:
  - python --version
  - curl https://gist.githubusercontent.com/BanzaiMan/450dab0b83251bb14369/raw/install_python.sh | bash -s 3.4.0
  - source $HOME/virtualenv/python3.4.0/bin/activate

script:
  - python --version

