language: c

sudo: required
dist: trusty

install:
  - true
script:
  - echo $PATH
  - ls -l ~/virtualenv/*/bin/
  - source ~/virtualenv/python2.7/bin/activate
  - deactivate
  - echo $PATH
  - which python
  - which python3
