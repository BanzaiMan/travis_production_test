language: ruby

rvm:
  - default

sudo: required
dist: trusty

addons:
  mariadb: '10.1'

install:
  - true

script:
  - cat /var/log/mysql.err
  - cat /var/log/mysql.log

notifications:
  email: false

