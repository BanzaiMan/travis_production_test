language: ruby

sudo: false

cache:
  branch: md5deep_debug
  directories:
    - $HOME/.gradle

install:
  - touch $HOME/.gradle/foo
  - echo "bar" >> $HOME/.gradle/foo

script:
  - true

before_cache:
  - cat $CASHER_DIR/mtime.yml

