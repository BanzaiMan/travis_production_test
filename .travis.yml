language: ruby

rvm:
  - "1.9.3"

install:
  - mkdir foo

script:
  - true

deploy:
  provider: gae
  oauth_refresh_token: deadbeef0123456789
  skip_cleanup: true
  app_dir: foo # defaults to $TRAVIS_BUILD_DIR
  edge: true
