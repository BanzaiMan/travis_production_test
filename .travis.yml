language: phyton
sudo: required

notifications:
  email: false

install:
  - true
script:
  - dpkg -S /lib/x86_64-linux-gnu/libc.so.6
  - dpkg -l $(dpkg -S /lib/x86_64-linux-gnu/libc.so.6 | cut -f1 -d:)
