language: ruby

sudo: required

services:
  - docker
  
env:
  - GOOGLE_EMAIL=foobar@gmail.com
  - CI_GOOGLE_TOKEN=1234567890abcdef

before_install:
  - "echo \"$CI_GOOGLE_TOKEN\" > ~/gcloud.json"
  - "gcloud auth activate-service-account \"$GOOGLE_EMAIL\" --key-file ~/gcloud.json"
