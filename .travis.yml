language: php

sudo: false

env:
  - VERSION=5.5.28
install:
  - true

script:
  - pushd $HOME/.php-build
  - git checkout master && git reset --hard HEAD && git pull origin master
  - popd
  - cat ~/.php-build/share/php-build/definitions/$VERSION

