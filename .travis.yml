language: ruby

rvm: jruby-19mode

env:
  global:
    - JRUBY_OPTS="--server -Xcext.enabled=true -Xcompile.invokedynamic=false"

sudo: required

install: true

script:
  - jruby -v
  - unset JRUBY_OPTS
  - jruby -v

notifications:
  email: false
