language: cpp

script: $COMMAND

matrix:
  include:
    - compiler: clang
      env: COMMAND='./configure && make && make test'
    - compiler: gcc
      env: COMMAND='./configure && make && make test'
    - env: COMMAND='cpplint'
