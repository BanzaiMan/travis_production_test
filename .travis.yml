language: c

sudo: required
dist: trusty

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y python python3

install:
  - true
script:
  - echo $PATH
  - ls -l ~/virtualenv/*/bin/
