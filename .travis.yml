language: ruby
sudo: required

services:
  - docker

before_install:
  - sudo apt-get update
  - docker pull ubuntu

script:
  - docker build
# sudo: false
# dist: trusty

# env:
#   global:
#     - BUNDLE_PATH=/home/travis/.bundle
#     - BUNDLE_FOO=bar
# cache:
#   bundler

# script:
#   - env | grep BUNDLE
#   - export BUNDLE_PATH=/home/travis/.bundle
#   - env | grep BUNDLE
#   - bundle install --jobs=3 --retry=3 --deployment --path=${BUNDLE_PATH:-vendor/cache}

notifications:
  email: false
  # webhooks: http://requestb.in/16le4he1

  #  slack:
  #  rooms:
  #    - secure: "KZTfjxhkOdDDZYD2ThBILt2ShYtdEee3PudFZlQBfpxG9N470GfxnKVfOl7Odu4lN6cIupwC4S4BnehubEywSR99l60VmfhePUCTqDLYy+iR9+guTvaECdXdaau3jTKhFdu5u60ITsiw5Yb+hwAaGFO0HmQhqR9XVr/U8IGQlok="
  #  template:
  #    - "Build <%{build_url}|#%{build_number}> (<%{compare_url}|%{commit}>) of %{repository}@%{branch} by %{author} %{result} in %{duration} (elapsed time: %{elapsed_time}) subject: %{commit_subject} message: %{commit_message} foobar"
  #  on_success: always
