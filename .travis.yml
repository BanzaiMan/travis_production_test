language: ruby

sudo: false

install:
  - true

script:
  - true

deploy:
  provider: s3
  edge:
    branch: ha-test-s3-aws-sdk-v2
  access_key_id: "YOUR AWS ACCESS KEY"
  secret_access_key: "YOUR AWS SECRET KEY"
  bucket: "hiro-test-bucket"
  on:
    all_branches: true
