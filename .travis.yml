language: phyton
sudo: false

notifications:
  email: false

install:
  - true
script:
  - dkpg -S /lib/x86_64-linux-gnu/libc.so.6
  - dpkg -l $(dkpg -S /lib/x86_64-linux-gnu/libc.so.6 | cut -f1 -d:)
