language: cpp

compiler:
  - clang

install:
  - true

script:
  - echo $PATH
  - which clang
  - sudo echo $PATH
  - sudo which clang
  - sudo env | grep PATH
  - sudo -i echo $PATH
  - sudo -i which clang
  - sudo -i env | grep PATH
