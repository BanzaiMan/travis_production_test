language: ruby
sudo: false

install:
- true
script:
- true

before_deploy:
  - ls .

deploy:
  edge:
    branch: ha-feature-script-improvements
  provider: script
  script: show_php.sh
  on:
    all_branches: true
