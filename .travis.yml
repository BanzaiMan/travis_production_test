language: scala
dist: trusty
sudo: true
scala:
 - "2.11.7"
jdk:
 - oraclejdk7
 - openjdk7
 - oraclejdk8
 - openjdk8

install: true

script:
  - hostname

addons:
 #workaround for openjdk buffer overflow
 hostname: dummy
