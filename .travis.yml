language: ruby

sudo: false

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    - llvm-toolchain-precise
    - llvm-toolchain-precise-3.6
    - debian-sid
    packages:
      - thrift-compiler

install: true

script:
  - true
