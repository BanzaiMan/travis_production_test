language: java

sudo: false

cache:
  branch: md5deep_debug
  directories:
    - foo
    - bar

install:
  - ls foo bar
  - touch foo/bar
  - touch bar/baz

script:
  - true
  
before_cache:
  - cat $CASHER_DIR/md5sums
  - cat $CASHER_DIR/paths
