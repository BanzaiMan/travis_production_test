language: php

php:
  - '5.6'

sudo: required
dist: precise

install:
  - true

script:
  - "ruby -rsocket -rtimeout -e 'Timeout::timeout(30) { puts TCPSocket.new(\"smtp.mandrillapp.com\", 587) }'"

notifications:
  email: false
