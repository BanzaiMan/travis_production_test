sudo: false
language: ruby

addons:
  apt:
    sources: 
      - elasticsearch-2.0
    packages:
      - elasticsearch

services:
  - elasticsearch
install: true

script: 
  - elasticsearch -v
  - curl http://localhost:9200/

