language: ruby

sudo: false

rvm:
  - 2.1.5

before_install:
  - true

script:
  - true

deploy:
  provider: script
  script: rvm 1.9.3 do bundle exec cap integration deploy
  skip_cleanup: true
  on:
    branch: default

