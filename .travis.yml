language: go

addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

sudo: false


install:
  - true

script:
  - find /etc/apt
  - cat /etc/apt/sources**/*.list
  - cat ~/apt-get-update.log
  - google-chrome --version
