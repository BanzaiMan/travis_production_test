language: ruby

rvm:
  - '2.1'

sudo: false

before_install:
  - sed -i -e 'd/rvm_autoupdate_flag/' ~/.rvmrc
  - rvm get head
  - rvm install 2.1 --latest
  - rvm use 2.1
  - ruby -v

install:
  - true
script:
  - true

