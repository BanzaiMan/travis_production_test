language: objective-c

rvm:
  - 2.1.5
  - 2.2.0

os: osx

install:
  - rm -rf foo* FOO*
  - touch foo FOO foo.txt foo.TXT FOO.txt FOO.TXT
  - ls -l foo* FOO*

script:
  - ruby -ve 'Dir.glob("*.txt")'
  - ruby -ve 'Dir.glob("foo.txt")'
  - ruby -ve 'Dir.glob("FOO.txt")'
