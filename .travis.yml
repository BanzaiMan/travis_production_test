language: ruby

rvm:
  - 2.1
  - 2.1.2

os:
  - linux
  - osx

install:
  - true

script:
  - echo "travis_fold:start:SCRIPT folding starts"
  - true
  - echo "travis_fold:end:SCRIPT folding ends"

matrix:
  allow_failures:
    - os: osx
