language: ruby


matrix:
  include:
    - sudo: required
      dist: precise
      env: RELEASE=precise
      rvm: 1.8.7-head
    - sudo: false
      rvm: 1.8.7-head

install:
  - true
script:
  - gcc --print-search-dirs
  - ldd `command -v ruby`
  - echo $LD_LIBRARY_PATH
