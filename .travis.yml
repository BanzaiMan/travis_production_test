language: php

sudo: false

matrix:
  include:
    - php: 5.6

install:
  - pushd $HOME/.php-build/bin
  - travis_wait env PHP_VERSION=master ./php-build -i development master $HOME/.phpenv/versions/master
  - phpenv global 7
  - php --version
  - popd

script:
  - true
