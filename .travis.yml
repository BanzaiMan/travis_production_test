language: ruby

sudo: false

addons:
  apt:
    sources:
      - travis-ci-sqlite3
    packages:
      - sqlite3

rvm: default

install:
  - true

script:
  - cat ~/apt-get-update.log
  - sqlite3 --version
