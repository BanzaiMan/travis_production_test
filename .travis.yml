language: ruby

os: osx

osx_image: xcode7

install: true

script:
  - rvm use 1.9.3
  - gem list
  - true

deploy:
  provider: s3
  access_key_id: "YOUR AWS ACCESS KEY"
  secret_access_key: "YOUR AWS SECRET KEY"
  bucket: "S3 Bucket"
  on:
    all_branches: true

