language: ruby

addons:
  postgresql: "9.3"

before_install:
  - sudo service stop postgresql
  - sudo rm -rf /var/ramfs/postgresql
  - sudo service start postgresql 9.1
  - sudo service stop postgresql
  - sudo service start postgresql 9.3

script:
  - ps aux | grep -v grep | grep postgres
