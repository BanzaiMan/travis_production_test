language: ruby

rvm:
  - 2.1.5
  
sudo: false

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
      - debian-sid
    packages:
      - gcc-4.8
      - g++-4.8
      - python2.7
      - wget
      - pkg-config
      - zip
      - zlib1g-dev
      - libunwind8-dev

install:
  - true
script:
  - true
  
