language: go

addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

sudo: false

install:
  - true

script:
  - google-chrome --version

after_failure:
  - tail /etc/apt/sources.list
