language: erlang

install:
  - true
  
script:
  - ssh-keygen -t dsa -f $HOME/.ssh/id_rsa -P ''
  - cp $HOME/.ssh/id_rsa.pub $HOME/.ssh/authorized_keys
  - ssh -N -f localhost
  - 'ps -ef | grep ssh'
