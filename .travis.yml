language: scala
dist: trusty
sudo: true
scala:
 - "2.11.7"
jdk:
 - openjdk7

before_install:
  - cat /etc/hosts

install: true

script:
  - hostname

addons:
 #workaround for openjdk buffer overflow
 hostname: dummy
