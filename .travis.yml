language: ruby


matrix:
  include:
    - sudo: required
      dist: trusty
      env: RELEASE=trusty
    - sudo: required
      dist: precise
      env: RELEASE=precise

install:
  - echo $LD_LIBRARY_PATH
    rvm $TRAVIS_RUBY_VERSION do echo $LD_LIBRARY_PATH
script:
  - true
