language: ruby
sudo: required

services:
  - docker

gemfile: path/to/Gemfile
before_install: cd $(dirname $BUNDLE_GEMFILE)
script: docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
