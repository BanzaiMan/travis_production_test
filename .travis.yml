language: ruby

sudo: false

deploy:
  - provider: script
    edge:
      source: travis-ci/dpl
    script: echo "foo"
  - provider: script
    edge:
      source: travis-ci/dpl
    script: echo "bar"

notifications:
  email: false
