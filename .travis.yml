language: python

matrix:
  include:
    - sudo: required
      services:
        - docker
      before_install:
        - sudo apt-get install alex
    - sudo: false
      addons:
        apt:
          packages:
            - alex

install:
  - true

script:
  - true
