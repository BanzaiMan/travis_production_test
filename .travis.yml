language: php

php:
  - '5.5'

sudo: required
dist: precise

before_install:
  - find ~/.phpenv/versions/$(phpenv version-name) -name \*.so -print -exec ldd {} \;
  
install:
  - true
script:
  - true
