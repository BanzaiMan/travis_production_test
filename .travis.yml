language: ruby

rvm:
  - jruby

sudo: false

env:
  global:
    - JRUBY_OPTS=--2.0

install:
  - bundle install --jobs=3 --without production development
  
script:
  - bundle --version
  
cache: bundler

