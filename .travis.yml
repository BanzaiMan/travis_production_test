language: node_js

sudo: false

node_js:
  - stable

before_install:
  - curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.29.0/install.sh | bash
  - nvm install stable
install:
  - true

script:
  - true
