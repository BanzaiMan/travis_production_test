language: ruby

sudo: false

cache:
  branch: md5deep_debug
  cache: bundler

script:
  - true

before_cache:
  - cat $CASHER_DIR/mtime.yml || true
  - cat $CASHER_DIR/md5sums || true
  - md5deep -x $CASHER_DIR/md5sums -r $HOME/.gradle || true

