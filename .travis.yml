language: python

python:
    - "2.7"

before_install:
    - export FOO=1
    - sudo usermod -a -G video $USER
script:
  - echo $FOO
  - sudo sudo -u $USER -g audio -E env FOO=$FOO bash -c 'groups; echo $FOO'

