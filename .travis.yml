language: ruby

os: osx

notifications:
  email: false

before_install:
  - find $rvm_path
  - find ~ -name \*bash\* -print -exec cat {} \;

install:
  - true

script:
  - true

after_failure:
  - find $rvm_path -name gem_make.out -print -exec cat {} \;
