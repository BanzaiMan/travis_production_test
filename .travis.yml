language: php

sudo: false

php:
  - 5.6
  - 5.5
env:
  - CUSTOM_RUBY_VERSION=2.1.5
  - CUSTOM_RUBY_VERSION=2.2.0

before_install:
  - rvm use $CUSTOM_RUBY_VERSION --install
  - ruby --version

script:
  - dpkg -l | grep libicu-dev
