language: c
sudo: required
services:
  - docker

before_install:
  - sudo -E apt-add-repository -y deb https://packagecloud.io/chef/current/ubuntu/ precise main
  - sudo apt-get update -qq

install:
  - sudo apt-get install pyzmq

script:
  - true

