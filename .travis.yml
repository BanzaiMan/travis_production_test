language: objective-c
sudo: false
before_install:
  - rvm 1.9.3 gem install net-ssh -v 2.9.2
install:
- true
script:
- true
deploy:
  provider: cloudfiles
  username: foo
  api_key: bardkfajd;fa
  region: 'foobar'
  container: 'foobar'
  on:
    repo: BanzaiMan/travis_production_test
    branch: default
