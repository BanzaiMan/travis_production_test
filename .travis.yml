language: php

php:
  - 5.3

before_install:
  - sudo apt-get install ruby-build

install:
  - phpenv help
  - phpenv versions
  - pushd ~/.phpenv; git pull; popd
  - phpenv install php-5.3.5

script:
  - true

