language: c

compiler:
  - gcc
  - clang
env:
  - 
  - SANITIZE=address
  - SANITIZE=undefined
matrix:
  exclude:
    - compiler: gcc
      env: SANITIZE=address
    - compiler: gcc
      env: SANITIZE=undefined

  allow_failures:
    - compiler: clang
      env: SANITIZE=address
    - compiler: clang
      env: SANITIZE=undefined
script: true
