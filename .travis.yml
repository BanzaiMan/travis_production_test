language: python

python:
  - '2.7'
  - '2.7.10'

sudo: required
dist: trusty

install:
  - true

script:
  - python -c "import sys; print ('UCS4 build' if sys.maxunicode > 65535 else 'UCS2 build')"

notifications:
  email: false

