language: node_js
node_js:
  - "0.10"
  - 0.11

matrix:
  allow_failures:
    - node_js: 0.11

services:
  - mongodb
  - redis-server

script: "npm test"

env:
  global:
    - DEFAULT_EXTERNAL=`ruby -e "puts Encoding.default_external"`

