language: ruby

rvm:
  - default
sudo: required
dist: trusty
group: edge

addons:
  apt:
    packages:
    - mysql-server-5.6
    - mysql-client-core-5.6
    - mysql-client-5.6
install: true
script:
  - ps -ef | grep -i mysql
  - sudo service mysql restart

notifications:
  email: false
