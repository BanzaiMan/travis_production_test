language: ruby

os:
  - linux

matrix:
  include:
    - os: linux
      rvm: 2.1
      env: STYLE_CHECKS=true

sudo: false

install:
  - true
  
script:
  - ruby -e "puts ENV.fetch('TRAVIS_OS_NAME')"
  
