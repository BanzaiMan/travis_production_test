language: ruby


matrix:
  include:
    - sudo: required
      dist: precise
      env: RELEASE=precise
      rvm: 1.8.7
    - sudo: false
      rvm: 1.8.7

install:
  - true
script:
  - ldd `command -v ruby`
  - echo $LD_LIBRARY_PATH
