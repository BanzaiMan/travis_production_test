language: python

python:
    - "2.7"

before_install:
    - export FOO=1
    - sudo usermod -a -G video,audio $USER
script:
  - echo $FOO
  - sudo -E su $USER -c 'groups; echo $FOO; exit 5'; return 0

