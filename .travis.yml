language: ruby

sudo: false

before_install:
  - true

script:
  - true

deploy:
  provider: script
  script: rvm 1.9.3 do bundle exec cap integration deploy
  skip_cleanup: true
  on:
    branch: default

