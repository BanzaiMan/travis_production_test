language: ruby

sudo: false

before_install:
  - echo "gem source 'https://rubygems.org'\ngem "capistrano"' > Gemfile

script:
  - true
  
after_success: "[[ $TRAVIS_BRANCH = 'default' ]] && gem install dpl && dpl --provider=script --script='bundle exec cap integration deploy'"
