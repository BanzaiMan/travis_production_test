language: php

sudo: false

matrix:
  include:
    - sudo: required
    - sudo: required
      services:
        - docker

install: true

script:
  - find / -name travis-ci-apache -print -exec cat {} \;
