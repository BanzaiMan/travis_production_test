language: ruby

os:
  - osx

rvm:
  - 2.1.1
  - 1.9.3

before_install:
  - if [ $TRAVIS_RUBY_VERSION = '1.9.3' ]; then rvm get head; rvm use 1.9.3-p545 --install; fi || true

install: true

script: true
