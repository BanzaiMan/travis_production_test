language: node_js
node_js:
  - "0.10"
  - 0.11

matrix:
  allow_failures:
    - node_js: 0.11

services:
  - mongodb
  - redis-server

script: "npm test"

env:
  global:
    - SERVER_HOST='127.0.0.1'
    - SERVER_OAUTH_GOOGLE_REDIRECTURI="http://localhost:400§cl/auth/google"

