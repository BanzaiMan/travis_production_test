language: ruby

os:
  - linux
  - osx

env:
  - VAR1=VAL1 VAR2=VAL2
  - VAR3=VAL3 VAR4=VAL4

matrix:
  include:
    - os: linux
      env: VAR1=VAL1 VAR2=VAL2
    - os: osx
      env: VAR3=VAL3 VAR4=VAL4

install: true

script:
  - env | grep VAR
