language: ruby

matrix:
  include:
  - os: linux
    sudo: required
    dist: precise
    group: edge
  - os: linux
    sudo: required
    services: docker
    group: edge


install:
  - true

script:
  - dpkg -l | grep -i haveged || true

notifications:
  email: false
