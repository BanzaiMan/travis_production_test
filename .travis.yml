language: python

notifications:
  email: false

sudo: required
dist: trusty

services:
  - elasticsearch

addons:
  apt:
    sources:
      - elasticsearch-1.7
    packages:
      - elasticsearch

install:
  - true

before_script:
  - sleep 10

script:
  - "curl http://localhost:9200/twitter"