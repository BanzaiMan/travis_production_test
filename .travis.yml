language: python

sudo: required
dist: precise

python:
  - '2.7'
  - '2.7.10'
  - '3.5'

install:
  - true

script:
  - python -c "import sys; print ('UCS4 build' if sys.maxunicode > 65535 else 'UCS2 build')"
