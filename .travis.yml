language: python

install:
  - sudo apt-get update -qq
  - sudo apt-get install python-scipy # install dependencies of scipy in one swoop
  - pip install scipy

script:
  - true

after_failure:
  - cat /home/travis/.pip/pip.log
