language: ruby

rvm:
  - default

sudo: required
dist: trusty

install:
  - true

script:
  - docker pull ubuntu
  - docker run --rm -it ubuntu bash -c "apt-get install -y curl && echo && curl -I yum.puppetlabs.com"

notifications:
  email: false

