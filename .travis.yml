language: python

notifications:
  email: false

sudo: false

services:
  - elasticsearch

install:
  - true

addons:
  apt:
    sources:
      - elasticsearch-1.7
    packages:
      - elasticsearch

script:
  - true

before_script:
  - sleep 10

install:
  - "curl http://localhost:9200/twitter"