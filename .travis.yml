language: ruby

rvm:
  - 2.1.5

os: osx

before_install:
- docker pull ubuntu

install:
- which brew || true

script:
  - docker run -v "$(pwd)":/app -w /app ubuntu ls -lad .
  - docker run -v "$(pwd)":/app -w /app ubuntu whoami
  - docker run -v "$(pwd)":/app -w /app ubuntu g++ -o /app/a.out main.cpp
  - docker run -v "$(pwd)":/app -w /app ubuntu g++ -o ~/a.out main.cpp
