sudo: false

language: ruby

install:
  - echo 'echo -e "\n${ANSI_GREEN}Log:${ANSI_RESET}\n"' > ansi_script
  - chmod +x ansi_script

script:
  - echo -e "\n${ANSI_GREEN}Hello there!${ANSI_RESET}\n"
  - travis_wait ./ansi_script

notifications:
  email: false
