language: objective-c

rvm:
  - 2.1.5

before_install:
  - ls -l ~/.ssh
  - cat /etc/ssh_config
  - cat ~/.ssh/known_hosts

install:
  - true

script:
  - rm ~/.ssh/known_hosts
  - git clone git@github.com:travis-ci/travis-web.git
