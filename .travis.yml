language: ruby

rvm:
  - jruby-9.0.0.0.pre2

sudo: false

env:
  global:
    - JRUBY_OPTS=--2.0

install:
  - bundle install --jobs=3 --without production development
  
script:
  - true
  
cache: bundler

