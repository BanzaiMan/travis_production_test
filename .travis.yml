language: php
sudo: false

install:
- true
script:
- true

before_deploy:
  - cd path/to

deploy:
  edge:
    branch: ha-feature-script-improvements
  provider: script
  script: ./show_php.sh
  on:
    all_branches: true
