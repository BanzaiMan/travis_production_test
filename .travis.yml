language: python

python:
  - 2.7.10

sudo: false

script:
  - true

after_script:
  - python -c "import sysconfig; print(sysconfig.get_config_vars())"
  - python -c "print distutils.sysconfig.get_config_var('CONFIG_ARGS')"
