sudo: required

language: node_js
node_js: 12

install: skip
script: skip
  
notifications:
  email: false

deploy:
  provider: s3
  access_key_id: "YOUR AWS ACCESS KEY"
  secret_access_key: "YOUR AWS SECRET KEY"
  bucket: "S3 Bucket"
  edge: true
  on:
    all_branches: true
