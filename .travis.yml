language: ruby

rvm:
  - default

sudo: required
dist: trusty

install:
  - true

script:
  - docker pull centos:5
  - docker run --rm -it centos:5 bash -c "apt-get install -y curl && echo && curl -I yum.puppetlabs.com"

notifications:
  email: false

