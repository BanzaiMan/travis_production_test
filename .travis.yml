language: ruby

rvm: '2.1.3'

sudo: required
dist: trusty

notifications:
  email: false

env:
  - FOO=var

before_install:
  - sudo apt-get update -qq >& /dev/null
  - sudo apt-get install docker-engine
  - sudo service docker restart

install:
  - true

script:
  - docker --version
