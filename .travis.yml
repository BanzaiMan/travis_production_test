language: cpp
sudo: false
dist: trusty

addons:
  sonarqube:
    branches:
    - jalazawaSonar
    token:
    secure: "444444"

jdk:
  - oraclejdk8

script:
  #    other script steps might be done before running the actual SonarQube analysis
  - sonar-scanner
  

cache:
  directories:
    - '$HOME/.sonar/cache'

notifications:
  email: true
