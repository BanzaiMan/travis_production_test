language: php

php:
  - 5.3

before_install:
  - git clone https://github.com/sstephenson/ruby-build.git ~/.rbenv/plugins/ruby-build
  - bash -l 'phpenv install 5.3.5'

install:
  - phpenv help
  - phpenv versions

script:
  - true

