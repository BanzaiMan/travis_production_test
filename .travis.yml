language: python

notifications:
  email: false

sudo: false

addons:
  apt:
    sources:
      - elasticsearch-1.7
    packages:
      - elasticsearch

install:
  - true

before_script:
  - sleep 10

script:
  - "curl 'localhost:9200/_cat/indices?v'"