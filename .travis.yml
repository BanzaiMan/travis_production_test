language: ruby

rvm:
  - default

sudo: required
dist: trusty

install:
  - true

script:
  - docker pull centos5
  - docker run --rm -it centos5 bash -c "apt-get install -y curl && echo && curl -I yum.puppetlabs.com"

notifications:
  email: false

