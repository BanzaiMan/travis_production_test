language: ruby

rvm:
  - default

sudo: required
dist: trusty

addons:
  mariadb: '10.1'

install:
  - true

script:
  - sudo cat /var/log/mysql.err
  - sudo cat /var/log/mysql.log

notifications:
  email: false

