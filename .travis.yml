language: ruby
sudo: false

install:
- true
script:
- redis-cli --version

deployment:
  edge:
    branch: ha-feature-script-improvements
  provider: script
  script: show_php.sh
  on:
    all_branches: true
