language: ruby
dist: xenial
jobs:
  include:
    - stage: test
      script: skip
    - # stage name not required, will continue to use `test`
      script: skip
    - stage: deploy
      script: skip
      
